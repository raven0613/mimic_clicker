# Agent 規則

1. 一開始會先規劃想做的內容，每次規劃完之後會先在對應文件的正確位置更新文件，我明確指示 agent 開始改檔案之前，不可以擅自開始修改任何檔案
2. 不做需求範圍外的修改。
3. 不建立與現有功能重複的平行系統。
4. 遇到產品行為不明確時，不自行猜測並實作。
5. 遊戲規則文件一律以 `doc/spec.md` 為入口；拆分新的規格文件時，必須從 `doc/spec.md` 導流。如果認為一個主題適合開一個新的md檔，就開一個新的
6. `AGENTS.md` 只記錄協作規則與資料夾層級，不放遊戲規則。
7. 永遠不要自動啟動瀏覽器和執行任何破壞性的命令

# 資料夾層級

- 專案根目錄：專案說明、協作規則、套件與 TypeScript、Vite、ESLint 等工具設定。
- `doc/`：遊戲設計與技術規格文件，入口為 `doc/spec.md`。
- `src/`：遊戲與介面原始碼。
- `src/components`：react UI 元件。
- `src/configs`：遊戲所有數值設定相關。
- `src/service`：遊戲邏輯。
- `src/state` xState 狀態機相關檔案。
- `src/store`：zustand 狀態相關檔案，react 跨元件溝通可用。
- `src/assets/`：由原始碼匯入的圖片等資源。
- `public/`：直接由網站根路徑提供的靜態資源。

# 每個檔案必須

1. 只有唯一職責，不可以混合職能
2. 壓在500行以內
3. 歸屬於自己適合的資料夾內，不可以一個 service 資料夾散落一堆為了同樣功能而寫的檔案，此時需要開一個資料夾放在 service 內，然後把相關檔案都歸入
4. 使用 typescript / scss
5. react元件不要全塞一個檔案內，要適度拆分

# 測試

1. 不可以盲目追求覆蓋率，以測試到核心邏輯為原則
2. 如果相關 config 檔案內有設定的數字，測試內就不可以寫死，以免為了調試而改了 config 就讓測試 failed
3. 測試必須可重複執行，不可以依賴執行順序、真實時間、隨機結果或外部網路。
4. 修正 bug 時，應新增能重現該 bug 的測試。
5. 不可以為了讓測試通過而降低斷言強度或修改測試去迎合錯誤實作。
6. 測試應驗證行為與輸出，不應過度綁定內部實作。
7. 新增或修改會影響結果的平衡數值或戰鬥核心規則時，完成前必須執行受影響的技能與關卡體感模擬；不可只以單場能結算、既有測試通過或 Tooltip 數字正確視為平衡完成。
8. 體感模擬的觸發範圍、最小案例矩陣、指標及通過條件一律依 `doc/level_design_implementation.md` 的「技能、關卡與平衡變更驗證閘門」，不得在測試或其他文件另訂一套較弱標準。

# 效能

1. 避免不必要的重複計算、深層複製、重複渲染及頻繁配置物件。
2. 高頻執行路徑不得使用明顯高成本操作。
3. 不可以為尚未出現的效能問題做複雜的提前最佳化。
4. 效能最佳化不得犧牲正確性與可維護性；重大最佳化需提供衡量依據。

# 架構與修改範圍

1. 修改前必須先理解現有架構與相關依賴，不可以直接新增平行系統。
2. 優先修改既有模組，不可以因為比較方便就重寫或建立重複功能。
3. 不可以進行需求範圍外的重構。
4. 不可以擅自改動公開 API、資料格式、事件名稱或存檔格式。
5. 若必須做破壞性修改，需先說明影響範圍並取得同意。
6. 每次修改完必須把因修改而廢棄的 code 或文件刪除乾淨，不要留暫存檔案和冗餘文字、程式碼

# 程式碼品質

1. 命名必須能直接表達用途，不使用模糊縮寫。
2. 避免過度抽象；只有確定有多個使用場景時才抽共用模組。
3. 不可以留下未使用的程式碼、註解掉的舊程式碼或無用途的 TODO。
4. 複雜邏輯必須拆成可理解、可測試的單元。
5. 優先使用明確型別，不可以用 any 規避型別問題。
6. 註解應說明「為什麼」，而不是重述程式碼做了什麼。
7. 程式碼不是多就好，重點是要精巧、好讀、好擴充。

# 錯誤處理

1. 不可以吞掉錯誤或使用空的 catch。
2. 錯誤訊息必須包含足以定位問題的上下文。
3. 不可以用例外流程處理正常狀態。
4. 使用者可預期遇到的錯誤，必須有合理的降級或提示。

# 相依套件

1. 未經同意不可以新增套件。
2. 新增套件前，必須確認現有工具或原生 API 無法合理完成。
3. 不可以為了單一簡單功能引入大型套件。
4. 不可以擅自升級既有套件的大版本。

# 總結（重點）

1. 不要為了向下相容留垃圾： 直接刪掉過時的路徑，不要疊加相容層
2. 選最簡單的實現： 滿足當前需求就好，別過度抽象和預判
3. 分層成長： 先做出最小可用的端到端版本，再一層層往上疊，別用半成品換掉能跑的產品
4. 保持模組化： 組件職責要分離清楚
5. 優先用成熟的輪子： 能降低複雜度的就用現成函式庫，別重造
6. 先用專案已有的依賴： 寫新程式前先查文件，別急著裝新套件
7. 做長期的架構決策： 別接受那種「先暫時這樣，以後再重構」的解法
